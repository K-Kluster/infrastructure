apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: kaspad
  namespace: kaspa
spec:
  routes:
    - match: Host(`testnet10-wrpc.kasia.fyi`)
      kind: Rule
      middlewares:
        - name: testnet10-wrpc-rate-limit
        - name: testnet10-wrpc-inflight-limit
      services:
        - name: testnet10-kaspad-external
          kind: Service
          port: 17620
---
apiVersion: v1
kind: Service
metadata:
  name: kaspad-external
  namespace: kaspa
  labels:
    app: testnet10-kaspad
spec:
  selector:
    app: testnet10-kaspad
  ports:
    - name: wrpc-borsh
      port: 17620
      targetPort: wrpc-borsh
      protocol: TCP
    - name: grpc
      port: 16620
      targetPort: grpc
      protocol: TCP
