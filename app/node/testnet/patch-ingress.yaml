apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: kaspad
  namespace: kaspa
spec:
  routes:
    - match: Host(`dev-wrpc.kasia.fyi`)
      kind: Rule
      services:
        - name: dev-kaspad-external
          kind: Service
          port: 17110
---
apiVersion: v1
kind: Service
metadata:
  name: kaspad-external
  namespace: kaspa
  labels:
    app: dev-kaspad
spec:
  selector:
    app: dev-kaspad
