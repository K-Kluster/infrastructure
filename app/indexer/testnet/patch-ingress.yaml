apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: indexer
  namespace: kaspa
spec:
  routes:
    - match: Host(`dev-indexer.kasia.fyi`)
      kind: Rule
      services:
        - name: dev-indexer-external
          kind: Service
          port: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: indexer-external
  labels:
    app: dev-indexer
  namespace: kaspa
spec:
  selector:
    app: dev-indexer
